# Cloudflare Pages / Workers Configuration for Tomi's Special Bites
name = "tomis-special-bites"
pages_build_output_dir = "."
compatibility_date = "2026-07-24"

[site]
bucket = "."

[vars]
ENVIRONMENT = "production"
